apiVersion: v1
kind: Service
metadata:
  labels:
    app: pipelines-web
  name: pipelines-web
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: pipelines-web